<h1>Les statistiques annuelles des entreprises</h1>
<table class="table">
    <thead>
    <tr>
        <th>Entreprise</th>
        <th>Année</th>
        <th>Note min.</th>
        <th>Note max.</th>
        <th>Note moy.</th>
    </tr>
    </thead>
    {% for note in notes %}
        <tr>
            <td>{{ note['entreprise'] }}</td>
            <td>{{ note['annee'] }}</td>
            <td>{{ note['min'] }}</td>
            <td>{{ note['max'] }}</td>
            <td>{{ note['moyenne'] }}</td>
        </tr>
    {% endfor %}
</table>